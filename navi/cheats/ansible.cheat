% ansible

# run playbook for inventory
ansible-playbook -i INVENTORY HOSTS_FILE

# run playbook for inventory, excluding host
ansible-playbook -i INVENTORY HOSTS_FILE --limit '!HOST'

# run playbook for inventory, limiting to a host
ansible-playbook -i INVENTORY HOSTS_FILE --limit 'HOST'
